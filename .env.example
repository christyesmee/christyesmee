# Copy to ".env" for local dev, or set these in Netlify (Site settings → Env vars).
# Without them the app still runs; the leaderboard just stays local-only.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
