# Netlify configuration for the Vite + React app.

[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send all routes to index.html so deep links work
# (e.g. /a1/1 or /leaderboard) without a 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
